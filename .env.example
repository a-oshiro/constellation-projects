# Copy to .env.local (gitignored) and fill in real values — never commit real secrets.
# No VITE_ prefix: this is read server-side only (api/fetch-urls.ts), never bundled into client JS.
OPENAI_API_KEY=
